-- COUNT, SUM, MIN, MAX, AVERAGE

-- COUNT requires AS alias, function notation COUNT(parameter)



-- NULL (empty data type)
-- N.B. IS NULL instead of = NULL, IS NOT NULL instead of != NULL
-- used in WHERE clauses as a condition

